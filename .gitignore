# macOS
.DS_Store
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs and temp files
*.log
npm-debug.log*
*.tmp
*.bak

# Node
node_modules/
dist/

# Python
.pytest_cache/
__pycache__/
*.pyc
*.pyo
*.pyd
QA/tests/__pycache__/

# Visual regression artifacts generated during local test runs.
QA/tests/visual-current/
QA/tests/visual-diff/

# Optional QA execution artifacts.
QA/results/*
!QA/results/.gitkeep
!QA/results/README.md
QA/results/playwright/.last-run.json

# Playwright runtime artifacts and duplicated local output folders.
test-results*/
.npm-cache/

# .claude/ holds machine-local Claude Code config. settings.local.json carries
# personal permission overrides and must stay untracked, but settings.json
# (the Drive-drift SessionStart hook) and launch.json (dev-server config)
# have no secrets and must be shared, or they silently vanish on every fresh
# clone -- exactly the gap that let this repo's .git corrupt three times
# before anyone noticed the guardrail wasn't actually wired up.
.claude/*
!.claude/settings.json
!.claude/launch.json
